# Weather rules

## Agent settings

enabled: true
uvHighThreshold: 6
precipChanceThresholdPercent: 40

Forecast location is fixed to **Paris** (`Europe/Paris`).

Optional override in `.env`: `WEATHER_LATITUDE`, `WEATHER_LONGITUDE`.
